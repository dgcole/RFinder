image: gradle:jdk10

stages:
  - build

gradle-build:
  stage: build
  before_script:
    - su -
    - apt-get install openjfx -y
  script:
    - gradle jar
  artifacts:
    paths:
      - target/*.jar
  only:
    - master